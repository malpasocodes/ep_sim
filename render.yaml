services:
  - type: web
    name: earnings-premium-sim
    env: python
    plan: free
    buildCommand: uv pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: STREAMLIT_BROWSER_GAP
        value: "0"
